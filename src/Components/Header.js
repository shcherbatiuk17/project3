// Import the necessary React module to create components.
import React from 'react';

// Define a functional component called "Header."
const Header = () => {
    // Render the following JSX structure when the component is used.
    return (
        // Create a header element to represent the page header.
        <header>
            {/* Display the main title "Tech Blog" within an h1 element. */}
            <h1>Tech Blog</h1>
        </header>
    );
}

// Export the "Header" component to make it available for use in other parts of the application.
export default Header;
